//File to contain all of the methods which will check the letters guessed versus the random word selected

var letters = require('./letter.js');

var Word = function(guess){
 this.guess = guess
  //check if letter is in secret word
     // for (var j = 0; j < word.length; j++){
       // if(word[j] === guess) {
         // answerArray[j] = guess;
  }


console.log("guess");

module.exports = Word;